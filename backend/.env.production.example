# =============================================================================
# PingLet Backend — Production Environment
# =============================================================================
# Copy this to .env.production and fill in all values before deploying.
#   cp backend/.env.production.example backend/.env.production
# =============================================================================

# --- Database ----------------------------------------------------------------
POSTGRES_DB=pinglet_prod
POSTGRES_USER=pinglet
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
DATABASE_URL=postgresql://pinglet:CHANGE_ME_TO_A_STRONG_PASSWORD@postgres:5432/pinglet_prod

# --- Redis -------------------------------------------------------------------
REDIS_URL=redis://redis:6379

# --- Auth --------------------------------------------------------------------
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL
JWT_EXPIRATION=900
JWT_REFRESH_EXPIRATION=1209600
PLUS_REVERSE_TRIAL_DAYS=7
GOOGLE_PLAY_BILLING_ENABLED=false
PLAY_REVIEW_EMAIL=
PLAY_REVIEW_OTP=

# Generate with: openssl rand -hex 32 (sent in the x-admin-key header)
ADMIN_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL

# Generate with: openssl rand -hex 32
OTP_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL

# --- App ---------------------------------------------------------------------
APP_PORT=3000

# --- OpenAI ------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_EXTRACTION_MODEL=gpt-5.4-mini
OPENAI_TRANSCRIPTION_MODEL=gpt-4o-transcribe
INGESTION_CONCURRENCY=2
CATALOG_AUTO_PROMOTION_ENABLED=true
CATALOG_EXTRACTION_MIN_CONFIDENCE=0.82
CATALOG_MATCH_MIN_CONFIDENCE=0.90
CATALOG_MATCH_MAX_ASSIGNMENTS=3

# --- Email (Zoho SMTP) -------------------------------------------------------
SMTP_HOST=smtppro.zoho.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=hello@pinglet.ai
SMTP_PASSWORD=
EMAIL_FROM=PingLet <hello@pinglet.ai>
EMAIL_REPLY_TO=hello@pinglet.ai
EMAIL_OTP_DEV_MODE=false

# --- Google Play Billing -----------------------------------------------------
GOOGLE_PLAY_PACKAGE_NAME=ai.pinglet.app
GOOGLE_PLAY_SERVICE_ACCOUNT_JSON=
